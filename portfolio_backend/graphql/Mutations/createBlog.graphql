extend type Mutation {
    createBlog(input: CreateBlogInput! @spread): CreateBlogMutationResponse!
        @field(
            resolver: "App\\GraphQL\\Mutations\\CreateBlogMutation@createBlog"
        )
}
